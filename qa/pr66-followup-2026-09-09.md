# PR #66 评论、Windows 与依赖安全修复

## 证据与修复

- 评论 `3967198619`：SSE 将 `ServerResponse.write(false)` 误判为失败。改为等待 drain 后恢复轮询，已缓冲帧不重放；close/finish 清理计时器与监听器，持续堵塞 30 秒后释放连接。补模拟背压、撤销/关闭/超时和真实 HTTP 1 MiB 快照回归。
- Windows run `34338094344`：Insights 9 项失败。混合路径分隔符在原始字节哈希下不一致，使 SQLite 与 JSONL 归属不匹配；统一来源路径组件，parser version 2 触发旧 checkpoint 重建。不使用需要文件存在的 canonicalize。文件时间测试使用可写句柄，修复 Windows PermissionDenied。
- Dependabot #2–7：传递依赖 fast-uri 3.1.5 → 3.1.6、qs 6.15.3 → 6.16.0。
- `pnpm audit` 另发现 js-yaml 与 hono 告警，分别升级 4.3.1 → 4.3.2、4.13.3 → 4.13.5。仅对受影响范围设置安全版本 overrides，未升级其他直接依赖。

## 本机验证

- `pnpm install --frozen-lockfile`、`pnpm audit --audit-level low` 通过，零已知漏洞。
- `cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、`cargo fmt --all -- --check` 通过。
- `pnpm test`：桌面 666 通过，1 项既有真实 Claude opt-in 跳过；Web 127 通过。最后追加的真实大快照测试另行执行通过。
- 追加 v1 数据库 checkpoint 升级专项测试通过：旧 raw rollout ID 的模型/fallback 被重读替换，模型恢复、无重复统计，下一轮空闲无读取。
- `pnpm typecheck`、`pnpm format:check`、`pnpm build`、`pnpm build:web:hosted`、`git diff --check` 通过；生成协议无差异。
- 子代理分区修复并交叉复核；不跳过 Windows 测试、不削弱工作流或鉴权。

## 交付边界

本机是 macOS，实际 Windows x64 工作流仍须推送后复跑，不能据本机测试声称 Windows CI 已绿。GitHub 默认分支上的依赖告警须合并修复并重新扫描后消除，未手工 dismiss。未提交、推送、部署或触发真实 Agent 控制；保留无关设计稿和临时目录。

## 第二轮：Windows 剩余失败与新增评论

- Run `34340418549`：前轮路径关联及句柄权限回归已通过，剩余文件替换测试返回旧值 20 而非 40。Windows 创建时间不具备唯一文件身份，替换后可能保留；改用同一打开句柄的卷号与 128 位 File ID，失败传播错误，不降级为时间戳。覆盖 SQLite/WAL 指纹，补旧时间身份缓存只重建一次回归。
- 评论 `3967431357`：Claude 探测失败永久缓存。改为失败冷却 30 秒后重试；成功缓存策略及每次启动 Runner 的精确版本校验保持不变。测试覆盖重试恢复、冷却无进程风暴和未知版本拒绝。
- 评论 `3967431365`：禁用 LAN 服务后设置网卡列表停止刷新。LAN 设置订阅期间继续共用可见轮询，隐藏/锁屏暂停、卸载释放；本地内置 Web 空闲行为不变。测试覆盖新网卡、网卡移除、双订阅共享、隐藏和卸载。
- 本机 Rust 全工作区、Clippy、格式检查通过；追加 Insights 28、platform 31 项通过。Windows platform 的 tests 交叉检查和 Clippy 通过；Insights 交叉编译被 SQLite 缺少 Windows C `stdlib.h` 阻塞，实际 Windows 执行仍需 CI。
- 桌面 668 项通过、既有真实 Claude opt-in 1 项跳过；类型检查、桌面渲染构建、前端格式与 diff 检查通过。两位子代理分区修复，交叉复核无明确新增缺陷。
- 第二轮尚未提交或推送，新增评论未提前标记 resolved。

## 第三轮：启动恢复、SSE 开销与 Windows 超时夹具

- Windows run `34342201889`：文件身份相关测试已通过，x64 剩余失败为 `command_timeout_terminates_the_windows_job_object` 的 descendant 未就绪。两层 PowerShell 冷启动占用了原 12 秒超时；原延迟 marker 断言也不能可靠证明终止。改为 Job Object 附着后放行子进程、独立有界就绪等待，再执行生产共用的 100ms 超时逻辑；保留真实子进程句柄，在 job guard 仍存活时确认退出。
- 评论 `3967573227`：Claude 的同步启动失败不再永久置为 unsupported。恢复发送前状态并推进 revision，要求重新同步后显式重试；已有 worker 的投递失败仍为 outcome-unknown，禁止自动重放。缺失管道时先清理尚未收到输入的子进程。
- 评论 `3967573240`：SSE 不再逐分片编码和扫描全部已累积前缀。按新片段计数、完成行后拼接，保留字节上限和 Unicode 边界。4 MiB / 1 KiB 分片回归约束总编码量不超过输入两倍，不使用计时断言。
- 全量验证额外暴露文件锁释放竞态：并行 fork 可能暂留同一 open-file-description，仅关闭 owner 文件不会立即释放 flock。SessionLock 显式 unlock 后关闭；确定性 dup 回归验证旧继承描述符不阻止新 owner，也不释放新 owner 的锁。
- 本机 `cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、`cargo fmt --all -- --check` 通过；Claude runner 20 项通过。
- `pnpm test`：桌面 668 通过、既有真实 Claude opt-in 1 项跳过，Web 129 通过。`pnpm typecheck`、`pnpm format:check`、内置 Web、托管 Web 与桌面渲染构建通过，`git diff --check` 通过。
- Windows Insights tests 的 Rust 交叉检查通过（使用系统 SQLite pkg-config 元数据绕开缺失 Windows C SDK）；这不是 Windows 链接或执行验收，实际 x64 流水线仍须推送后确认。
- 本轮未 commit、push、部署或触发真实 Agent 控制；两条新增评论尚未提前标记 resolved。保留无关改动。
